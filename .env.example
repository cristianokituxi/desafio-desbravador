# GitHub personal access token (optional)
# Without a token: 60 requests/hour
# With a token:    5000 requests/hour
# Create one at: https://github.com/settings/tokens
# Required scopes: none (public repositories only)
VITE_GITHUB_TOKEN=
